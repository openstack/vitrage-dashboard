---
features:
  - A new ``Seach Entities`` was added, to find relevant entities in a graph.
    Search will highlight relevant entities in a styled circle. It will search
    objects by their names (As the user can see on screen)
