- project:
    templates:
      - openstack-python-jobs-horizon
      - openstack-python35-jobs-horizon
      - publish-openstack-sphinx-docs
      - check-requirements
      - release-notes-jobs
    check:
      jobs:
        - nodejs-npm-run-lint:
            vars:
              node_version: 4
            voting: false
        - nodejs-npm-run-test:
            vars:
              node_version: 4
            voting: false

